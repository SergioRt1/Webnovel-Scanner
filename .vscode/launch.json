{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: app.main (venv)",
            "type": "python",
            "request": "launch",
            "module": "app.main",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/venv/bin/python3.11",
            "cwd": "${workspaceFolder}"
        }
    ]
}